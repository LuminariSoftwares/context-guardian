__pycache__/
*.pyc
.venv/
venv/
env/
.env
logs/
*.log
.pytest_cache/
.DS_Store

# Working scratch -- editor backups, staged deletions, and the
# runtime log/span directory that now defaults inside the repo.
*.backup-*
_to_delete*/

# python build artifacts
dist/
build/
*.egg-info/

# node (DSH bundle dev install)
node_modules/

# local pre-edit backups
_archive/

# library: no lockfile
pnpm-lock.yaml
